{
  "name": "@anijs/eslint-config-monorepo",
  "version": "1.0.0",
  "private": "true",
  "description": "eslint config preset",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "@anijs/prettier": "^1.0.2",
    "eslint": "^8.2.0"
  }
}
