{
  "name": "@anijs/eslint-config-monorepo",
  "version": "1.0.0",
  "private": "true",
  "description": "eslint config preset",
  "license": "ISC",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "release": "lerna publish --conventional-commits",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@anijs/eslint-config-common": "workspace:*",
    "@anijs/prettier": "^1.0.3",
    "eslint": "^8.2.0",
    "lerna": "^3.22.1",
    "prettier": "^2.4.1"
  }
}
